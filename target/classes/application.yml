spring:
  application:
    name: webtruyenapi
  
  datasource:
    url: jdbc:sqlserver://SAUSLAPTOP\SQLEXPRESS:1433;databaseName=truyen;trustServerCertificate=true
    username: sa
    password: 123456
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
        format_sql: true
        show_sql: false
  
  mail:
    host: smtp.gmail.com
    port: 587
    username: Your_Email
    password: Your_Email_Password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
  
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

jwt:
  key: ThisIsASecretKeyForJwtToken123456
  issuer: MyApp
  expiration: 86400000 # 24 hours in milliseconds

google:
  auth:
    clientId: Your_Google_Client_ID

logging:
  level:
    root: INFO
    com.webtruyenapi: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
